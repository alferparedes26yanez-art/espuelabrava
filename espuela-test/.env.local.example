# Configuración para usar Firestore en lugar de mocks
# Copia este archivo a .env.local y ajusta los valores

# Activar Firestore (true) o usar mocks (false o no definido)
NEXT_PUBLIC_USE_FIRESTORE=false

# Las credenciales de Firebase ya están en lib/firebase.ts
# No necesitas configurar nada más aquí para el cliente

# Para operaciones de servidor/admin (opcional):
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
